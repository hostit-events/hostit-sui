// Registers @testing-library/jest-dom matchers (toBeInTheDocument, etc.) for the
// jsdom environment used by Vitest. Imported via vitest.config.ts `setupFiles`.
import "@testing-library/jest-dom/vitest";
