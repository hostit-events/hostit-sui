# Implementation Plans

Generated by the `improve` skill on **2026-06-20** from a 10-perspective review
(Designer, QA, Systems, SWE, Power-user, Regular-user, Security, Performance),
each finding adversarially verified, then authored by one writer per plan from
first-hand file reads.

**Planned at commit `957206b`** ("feat(forum): organizer admin [#37]"). HEAD
moved from `9b169c0` → `957206b` mid-authoring; every plan's drift-check is
pinned to `957206b`. One file (`017`) labels its Status `9b169c0` but its
excerpts and drift-check use `957206b` — treat `957206b` as the baseline.

Each executor: read the plan fully, run its drift-check first, honor STOP
conditions, and update your row below. **`009` and `010` are DECISION plans —
they STOP for a maintainer choice before any code.** **`007`, `008`, `011` (and
deploying any Move change) are gated package UPGRADES requiring explicit
per-deploy authorization per CLAUDE.md — code + tests may land, deployment may not.**

## Execution order & status

Recommended order: **001 first** (it's what makes every other plan's tests an
actual merge gate), then security quick-wins, then the systemic `012`, then the
rest. Numeric order below ≈ priority order.

| Plan | Title | Pri | Effort | Risk | Depends on | Status |
|------|-------|-----|--------|------|------------|--------|
| 001 | Frontend CI gate (tsc/lint/vitest on `web/**`) | P1 | S | LOW | — | DONE |
| 002 | Sponsor routes: auth/rate-limit/body-cap + stop error leak | P1 | S | LOW | — (see 003) | DONE |
| 003 | Durable KV rate-limiter + replay nonce | P1 | M | MED | after 002 | DONE (KV inert until RATE_LIMIT_KV_REST_* provisioned) |
| 004 | Security headers / CSP | P1 | M | MED | — | DONE |
| 005 | Seal `verifyKeyServers` driven off NETWORK | P2 | S | LOW | — | DONE |
| 006 | Copilot route: error hygiene + content clamp | P2 | S | LOW | — | DONE |
| 007 | Seal organizer-policy namespace separation | P2 | S | LOW | upgrade-gated | DONE (source+tests; deploy gated) |
| 008 | Move quick correctness (overflow, zero-bet, signer-emit, doc) | P1 | S | LOW | upgrade-gated | DONE (source+tests; deploy gated) |
| 009 | Refund fee policy → Option B (disclose forfeit) | P1 | M | MED | default chosen | DONE (source+tests; deploy gated) |
| 010 | Per-day check-in unit → PER-TICKET | P2 | M | MED | default chosen | DONE (source+tests; deploy gated) |
| 011 | Parimutuel dust → fold into final winner's claim | P2 | M | MED | upgrade-gated | DONE (source+tests; deploy gated) — fold-into-final-winner, no struct change, no new fn |
| 012 | Capped-page reads → cursor enumeration + truncation banner | P1 | M | LOW | — | DONE |
| 013 | Real scannable ticket QR (add generator) | P1 | S | LOW | — | DONE |
| 014 | `humanizeError` predict codes 7/8 | P2 | S | LOW | partial until FE-1 (deferred) | DONE |
| 015 | Testnet banner visible on mobile | P2 | S | LOW | — | DONE |
| 016 | Buy balance pre-check + faucet hint | P2 | S | LOW | — | DONE |
| 017 | Mobile-correct empty-wallet guidance | P2 | S | LOW | — | DONE |
| 018 | Cover-image perf attrs (lazy/decode/dims) | P2 | S | LOW | — | DONE |
| 019 | react-query global retry cap | P2 | S | LOW | — | DONE |
| 020 | Config/doc package-id drift + pin framework SHA | P1 | S | LOW | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

**Implemented on branch `advisor/implement-plans`** (gates green: `sui move test` 103/103, web `tsc` clean + `lint` 0 errors + 109 vitest). **20/20 DONE.** Decision plans resolved by the controller's default (reversible): 009 = Option B (disclose the non-refundable fee, no fund-flow change); 010 = PER-TICKET dedup. The Move plans (007–010) have code+tests landed and passing, but on-chain **deployment (`sui client upgrade`) is gated** and awaits explicit authorization. KV-backed rate limiting (003) is inert until `RATE_LIMIT_KV_REST_*` env vars are provisioned in the deploy environment.

## Dependency & sequencing notes

- **001 → everything testable.** It's the prerequisite that turns the new
  vitest/tsc gates in 002–020 into real merge protection. Land it first.
- **002 then 003.** 002 adds the per-IP `rateLimit()` call to the sponsor routes
  using the existing in-memory limiter; 003 makes that limiter durable across
  serverless instances **and** adds the memory-auth replay nonce (both need the
  same KV store). The limiter gates **5** routes (copilot, memory recall/remember/
  analyze, create-assist), not the 2 the original finding implied.
- **007, 008, 011 are Move changes.** Code + tests can land, but deploying them is
  a `sui client upgrade` (this is a fresh v1 — it would be the *first* upgrade),
  which needs explicit per-deploy authorization. Plans scope deployment OUT.
- **009, 010 STOP for a decision first.** 009: are platform fees refundable? (plan
  recommends the localized "disclose the forfeit" option A2 / B over re-architecting
  escrow). 010: is check-in per-ticket or per-attendee? Both write both branches.
- **012 also closes** the sellout-market discovery divergence and the per-event-page
  double-fetch (folded in). Caveat baked into the plan: `useAllEvents` takes no
  options, so the naive swap drops Door/Check-in 8s polling and Forum `enabled`
  gating — the plan restores liveness explicitly.
- **014 is only partial until FE-1 (deferred).** On the default gasless path, Enoki
  aborts bypass the humanizer entirely ("please retry"); mapping codes 7/8 fully
  pays off once that's fixed.
- **020 edits the git-ignored `web/.env.local`** (won't appear in `git status`) and
  flags that the file contains live-looking secrets (Enoki/Google/MemWal/Groq) —
  no values reproduced; rotate if ever exposed.

## Findings considered but NOT planned (deferred — do not re-audit)

Surfaced and vetted in the review; out of the three selected bundles
(quick-wins + security + Move). Each is a real, evidenced finding worth its own
plan later:

- **FE-1 — sponsored-abort humanizer bypass** (HIGH value; gates full value of 014).
- **FE-3** cross-query invalidation (stale sibling views ~15-30s); **FE-4** `useSuiQuery` `as unknown` casts erase RPC type-checking.
- **CFG-1** — after the first upgrade, core call targets stay pinned to original `PACKAGE_ID` vs the documented "calls target latest" rule (latent; ids equal today).
- **UX** — error boundaries (no `error.tsx`); `prefers-reduced-motion` ignored app-wide; inconsistent loading/empty/error + ARIA drift; OAuth-failure diagnosis; door-staff onboarding; dual styling systems (inline brand-vars + dead light token set).
- **PERF** — Discover N+1 Walrus metadata fetch (outside react-query); `motion` runtime in every route's first-load JS via `Icon`.
- **TST bundle** (gated by 001) — no positive `check_in` voucher success test (the staffed door happy-path is unasserted, finding Q1); untested `ticketing.ts` PTBs incl. `totalWithFee`; `staffKey` voucher byte-layout; sponsor routes; `seal_approve_*` gates; platform-fee over-withdraw; Walrus parse + refunded-ticket guard.
- **Direction (product)** — event edits unreachable (`updateMaxTicketsTx` is dead code); ticket resale dark (full Kiosk + royalty seeded on-chain); no signer-revoke UI; no cancel/bulk-refund; no cross-market "my bets" view.
- **Discovered while planning (012):** `foldModeration` keys by `target_blob_id` while `ForumScreen` looks up `blob_id` — possible key mismatch independent of truncation. Investigate.
- **Lower-confidence leads:** `minted` monotonic sold-out gate after refund (M5); `withdraw_platform_balance` event `to` vs actual destination (A4).
