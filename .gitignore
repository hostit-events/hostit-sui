# Dependencies
node_modules/

# Move build output (regenerated by `sui move build`)
/build/

# Next.js
.next/
*.tsbuildinfo
next-env.d.ts

# Local env / secrets — NEVER commit (keep the *.example template)
.env
.env*.local

# Sui automated-address-management manual backups (Published.toml itself IS committed)
Published.toml.*-backup

# OS / editor
.DS_Store
.vercel

# Local working docs / session records (ISSUES.md, .suiperpower) — not part of the repo
docs/

# Local-only symlink so the suiperpower skills + deploy gating still resolve .suiperpower at the repo root
/.suiperpower
